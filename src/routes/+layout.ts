// Disable SSR - this is a pure client-side SPA
// All data comes from Hyperliquid API, no server rendering needed
export const ssr = false;
export const prerender = true;
export const trailingSlash = 'always';
