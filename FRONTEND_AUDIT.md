# 前端审计记录 · 2026-09-05

## 范围与状态

上一批 HIP-3 市场筛选与元数据隔离已提交为 `608828a`。
本轮审查订单 REST/WS、地址切换、订阅生命周期、行情详情和 K 线切换；修复保留在工作区，便于单独审阅。
这不是完整的资金安全审计，也没有执行实盘下单、撤单、钱包授权或转账。

## 本轮已修复

- **HIP-3 挂单漏显示**：`ordersStore` 现在按协议注册表查询、订阅并合并各 DEX 的挂单。主 DEX 仍包含现货；市场页的四家展示名单不用于过滤账户已有挂单。显式按 DEX 刷新只更新对应部分，撤改单后默认刷新所有 DEX。
- **订单跨地址串数据**：切换地址时立即清空前一个地址的数据，旧会话的 REST、WS 和加载状态更新全部失效。相同地址的重复订阅初始化会复用；新 WS 快照优先于迟到的 REST 响应。
- **失败被当成空订单**：单个 DEX 请求失败不会阻止其他 DEX 和订阅初始化。界面提示缺失/过期的数据来源，禁用可能误导的 Cancel All。Refresh 会重新发现 DEX 并重建订阅；发现失败时先查询主 DEX 和四家展示 DEX，并明确提示旧 DEX 挂单可能缺失。
- **订阅清理竞态**：尚未返回的 SDK 订阅句柄也可被取消；迟到的旧句柄不能覆盖新订阅，旧句柄的 abort 不能删除其替代者。订单流运行时失败会保留会话级错误提示。
- **行情切换串数据**：订单簿与逐笔成交忽略旧币种回调。详情页切换/卸载后不再续建旧订阅，实时价格上下文绑定币种。钱包晚连接或切换时会同步更新详情页订单会话。
- **K 线切换竞态**：币种、周期变化时重新加载并清空旧图；旧快照、旧 WS 回调不能覆盖当前图。卸载期间仍在加载的模块/请求不能重新创建图表或订阅。
- **成交合并**：延迟返回的 REST 成交历史不会覆盖较新的 WS 成交；去重键包含币种、成交 ID 和买卖方向，保留自成交两侧。

接口语义对照了 [Hyperliquid Info 文档](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint) 和 [WebSocket 订阅文档](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions)，并核对了仓库安装的 SDK 源码。空字符串 DEX 只代表主 DEX，不代表跨 DEX 聚合。

## 高优先级发现：后续修复已完成

以下两项在后续修复中已处理，并补充自动化回归测试；仍未进行真实账户交易验证。

1. **已修复：资产/余额状态未按地址隔离。** 账户快照按规范化地址保存，保证金再按 DEX 区分。`forAddress(walletAddress)` 是交易表单、杠杆控件及市场列表读取钱包数据的唯一入口，默认 getters 只读取显式选择的查看地址；钱包请求不会改变查看地址。`loadAddress` 同步切换并清空新视图，REST 使用请求版本，WS 使用会话及归属检查；A→B→A、重置、晚到回调不会覆盖新快照。完整 WS 空快照会清除旧持仓，不会从旧 REST 数据中恢复已消失的持仓。请求失败明确显示错误并结束加载。Portfolio 中钱包连接仅在没有显式查看地址或深链接时充当默认地址，不再夺走用户选择。
2. **已修复：HIP-3 下单参考价回退到旧元数据。** 交易专用价格改为当前币种 `l2Book` 最优买卖报价的中价，适用于 HIP-3、主 DEX 和现货，不再回退到 `allMids`、元数据或 mark price。报价必须有有效双边盘口，服务端时间及本地接收时间均在 15 秒有效窗口内（最多允许 5 秒服务端超前时钟偏差）；空盘口、错误数据、过期、socket close/error 或订阅失败均不能产生可交易报价。重连清空报价并更换连接代次，必须等待新盘口；界面显示报价状态与年龄，并提供 Refresh feed。

市价单在解析资产 ID 后、创建确认框时和实际提交前都会再次验证报价。确认框固定钱包客户端/地址、币种、现货标识、价格、数量、方向及 reduce-only 参数；钱包或币种改变会取消确认，异步准备期间表单变化会要求重新检查。原确认报价过期或连接代次改变，即便已经收到了新报价，也必须重新确认，不会自动放宽或重新计算已确认的价格。重复点击确认不会重复发出订单。手动输入价格的限价单不依赖实时中价，但同样校验钱包和币种身份。

15 秒是本前端的保守交易时效阈值，不是交易所保证；不活跃市场可能需要手动刷新盘口后重新确认。页头 mark price 和交易表单 book mid 是不同价格类型，数值不必相同。

## 验证与限制

- `npm test`：100 个测试通过，本次新增 24 个（累计比 `608828a` 新增 48 个）。测试使用真实 Svelte 客户端响应式编译，HTTP/WS 为模拟输入；覆盖地址隔离、请求乱序、完整空快照、断线重连、报价时效和确认上下文变化。
- `npm run check`：0 errors，0 warnings。
- `npm run build`：通过；仍有依赖注释、超过 500 kB 的包体、Browserslist 数据过期等既有构建警告。
- `git diff --check`：通过。
- 尚未进行真实浏览器端到端测试、真实 WS 重连测试、依赖漏洞全量扫描或实盘交易验证。
